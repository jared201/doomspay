import { Button, VerticalBox, LineEdit, StandardButton } from "std-widgets.slint";
import { Page, ActionButton } from "mainpage.slint";

export AppWindow := Window {
    property<int> counter: 42;
    callback request-increase-value();
    title: "Doomspay";
    width: 250px;
    height: 200px;
    ActionButton {
         icon: @image-url("images/usb.svg");
         text: "Plug in your hardware wallet";

      }



}
